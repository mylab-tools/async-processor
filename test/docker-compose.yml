version: '3.2'

services:
  redis:
    container_name: redis
    image: redis:6
    ports: 
    - 10201:6379

  rabbit:
    container_name: rabbit
    image: rabbitmq:3.8.3-management
    ports:
      - "10202:5672"
      